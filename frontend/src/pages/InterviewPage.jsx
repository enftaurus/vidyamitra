import InterviewConsole from '../components/InterviewConsole';

export default function InterviewPage({ title, basePath, roundKey }) {
  return <InterviewConsole title={title} basePath={basePath} roundKey={roundKey} />;
}
